# include<stdio.h>
int main()
{
	int nums[3] = {3,2,4,};
	int target = 6;
	int i,j;
	for(i=0 ; i<2 ; i++)
	{
		for(j=2 ; j>0 ; j--)
		{
			if(nums[i] + nums[j] == target)
			{
				printf("[%d, %d]", i,j);
				break;
			}
		}
	} 
	return 0;
}
